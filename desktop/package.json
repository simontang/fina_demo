{
  "name": "evario-desktop",
  "productName": "Scale",
  "version": "0.1.2",
  "private": true,
  "description": "Scale desktop shell (remote Agent Gateway)",
  "author": "Alphafina",
  "main": "main.js",
  "scripts": {
    "dev": "RENDERER_DIR=../ai_web/dist electron-forge start",
    "pack": "electron-forge package",
    "make": "electron-forge make",
    "dist": "pnpm --dir ../ai_web run build && electron-forge make",
    "clean": "rimraf out"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "http-proxy": "^1.18.1"
  },
  "devDependencies": {
    "@electron-forge/cli": "^7.6.0",
    "@electron-forge/maker-dmg": "^7.6.0",
    "electron": "^37.0.0"
  }
}
