services:
  b1s:
    image: finai-cn-shanghai.cr.volces.com/default/fina-demo-b1s:latest
    environment:
      SERVER_PORT: "5705"
      SPRING_DATASOURCE_URL: ${SPRING_DATASOURCE_URL:-jdbc:postgresql://pgm-uf615169n98t95tflo.pg.rds.aliyuncs.com:5432/postgres}
      SPRING_DATASOURCE_USERNAME: ${SPRING_DATASOURCE_USERNAME:-postgres_fuli}
      SPRING_DATASOURCE_PASSWORD: ${SPRING_DATASOURCE_PASSWORD:-jfd4whz@GPF2nqx7zhm}
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: org.postgresql.Driver
      METRICS_DATASOURCE_TYPE: SQLSERVER
      ENCRYPT_KEY: ${ENCRYPT_KEY:-fina-metrics-2024!}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      LOG_DIR: /tmp/b1s-logs
      SKIP_INIT: ${SKIP_INIT:-false}
      B1_SERVICE_LAYER_BASE_URL: ${B1_SERVICE_LAYER_BASE_URL:-https://tengnat.yiyuntong.net:13450/b1s/v1}
      B1_COMPANY_DB: ${B1_COMPANY_DB:-XSM_ZSK}
      B1_USERNAME: ${B1_USERNAME:-manager}
      B1_PASSWORD: ${B1_PASSWORD:-1124}
      MAIL_LISTENER_ENABLED: ${MAIL_LISTENER_ENABLED:-false}
      MAIL_IMAP_HOST: ${MAIL_IMAP_HOST:-imap.larksuite.com}
      MAIL_IMAP_PORT: ${MAIL_IMAP_PORT:-993}
      MAIL_USERNAME: ${MAIL_USERNAME:-}
      MAIL_PASSWORD: ${MAIL_PASSWORD:-}
      MAIL_FOLDER: ${MAIL_FOLDER:-INBOX}
      MAIL_POLL_INTERVAL: ${MAIL_POLL_INTERVAL:-60s}
      MAIL_POLL_INTERVAL_MS: ${MAIL_POLL_INTERVAL_MS:-60000}
      MAIL_BATCH_SIZE: ${MAIL_BATCH_SIZE:-20}
      MAIL_MARK_SEEN: ${MAIL_MARK_SEEN:-true}
      TOS_ENABLED: ${TOS_ENABLED:-false}
      TOS_ENDPOINT: ${TOS_ENDPOINT:-}
      TOS_REGION: ${TOS_REGION:-}
      TOS_ACCESS_KEY: ${TOS_ACCESS_KEY:-}
      TOS_SECRET_KEY: ${TOS_SECRET_KEY:-}
      TOS_BUCKET: ${TOS_BUCKET:-}
      TOS_KEY_PREFIX: ${TOS_KEY_PREFIX:-b1s/mail-attachments}
      S3_ENABLED: ${S3_ENABLED:-false}
      S3_ENDPOINT: ${S3_ENDPOINT:-}
      S3_REGION: ${S3_REGION:-}
      S3_ACCESS_KEY: ${S3_ACCESS_KEY:-}
      S3_SECRET_KEY: ${S3_SECRET_KEY:-}
      S3_BUCKET: ${S3_BUCKET:-}
      S3_KEY_PREFIX: ${S3_KEY_PREFIX:-b1s/mail-attachments}
      LLM_INTENT_ENABLED: ${LLM_INTENT_ENABLED:-false}
      LLM_INTENT_ENDPOINT: ${LLM_INTENT_ENDPOINT:-https://llm.alphafina.cn/v1/messages}
      LLM_INTENT_BEARER_TOKEN: ${LLM_INTENT_BEARER_TOKEN:-}
      LLM_INTENT_MODEL: ${LLM_INTENT_MODEL:-kimi-k2.6}
      LLM_INTENT_MAX_TOKENS: ${LLM_INTENT_MAX_TOKENS:-128}
      LLM_INTENT_ANTHROPIC_VERSION: ${LLM_INTENT_ANTHROPIC_VERSION:-2023-06-01}
      AGENT_RUN_ENABLED: ${AGENT_RUN_ENABLED:-false}
      AGENT_RUN_BASE_URL: ${AGENT_RUN_BASE_URL:-https://ada.alphafina.cn}
      AGENT_RUN_BEARER_TOKEN: ${AGENT_RUN_BEARER_TOKEN:-}
      AGENT_RUN_TENANT_ID: ${AGENT_RUN_TENANT_ID:-tenant_2}
      AGENT_RUN_WORKSPACE_ID: ${AGENT_RUN_WORKSPACE_ID:-}
      AGENT_RUN_PROJECT_ID: ${AGENT_RUN_PROJECT_ID:-}
      AGENT_RUN_ASSISTANT_ID: ${AGENT_RUN_ASSISTANT_ID:-v2-sales-order-orchestrator}
      AGENT_RUN_MODE: ${AGENT_RUN_MODE:-}
      AGENT_RUN_STREAMING: ${AGENT_RUN_STREAMING:-false}
      AGENT_RUN_BACKGROUND: ${AGENT_RUN_BACKGROUND:-true}
      AGENT_RUN_CONNECT_TIMEOUT_MS: ${AGENT_RUN_CONNECT_TIMEOUT_MS:-15000}
      AGENT_RUN_READ_TIMEOUT_MS: ${AGENT_RUN_READ_TIMEOUT_MS:-600000}
    ports:
      - "127.0.0.1:5705:5705"
    restart: unless-stopped
