# 系统样式配置指南

本文档包含了系统的所有样式配置，方便您查看和调整界面风格。

## 📋 目录

1. [颜色配置](#颜色配置)
2. [尺寸规范](#尺寸规范)
3. [组件样式](#组件样式)
4. [如何修改样式](#如何修改样式)

---

## 🎨 颜色配置

### 主色调 (Primary Colors)

| 变量名 | 当前值 | 用途 |
|--------|--------|------|
| `--primary-color` | `#3a71ff` | 主按钮、链接、选中状态 |
| `--primary-hover` | `#66b1ff` | 悬停状态 |
| `--primary-active` | `#3a8ee6` | 激活状态 |
| `--primary-bg-light` | `#ecf5ff` | 选中背景、高亮背景 |

### 功能色 (Functional Colors)

| 变量名 | 当前值 | 用途 |
|--------|--------|------|
| `--danger-color` | `#f56c6c` | 错误提示、删除按钮 |
| `--success-color` | `#67c23a` | 成功提示 |
| `--warning-color` | `#e6a23c` | 警告提示 |

### 文字颜色 (Typography Colors)

| 变量名 | 当前值 | 用途 |
|--------|--------|------|
| `--text-main` | `#303133` | 标题、主要文字 |
| `--text-regular` | `#606266` | 正文、常规文字 |
| `--text-secondary` | `#909399` | 次要文字 |
| `--text-placeholder` | `#c0c4cc` | 占位符、禁用文字 |

### 边框颜色 (Border Colors)

| 变量名 | 当前值 | 用途 |
|--------|--------|------|
| `--border-color` | `#dcdfe6` | 主边框 |
| `--border-color-light` | `#e4e7ed` | 次要边框 |
| `--border-color-lighter` | `#ebeef5` | 最浅边框 |

### 背景颜色 (Background Colors)

| 变量名 | 当前值 | 用途 |
|--------|--------|------|
| `--bg-color-base` | `#f5f7fa` | 页面背景、表头背景 |
| `--bg-color-container` | `#ffffff` | 容器背景 |
| `--bg-color-disabled` | `#f5f7fa` | 禁用状态背景 |
| `--bg-color-hover` | `#f5f7fa` | 悬停背景 |
| `--white` | `#ffffff` | 纯白背景 |

---

## 📏 尺寸规范

### 圆角 (Border Radius)

| 变量名 | 当前值 | 用途 |
|--------|--------|------|
| `--border-radius-base` | `4px` | 标准圆角（按钮、输入框、卡片） |
| `--border-radius-small` | `2px` | 小圆角 |
| `--border-radius-large` | `8px` | 大圆角 |

### 字体大小 (Font Size)

| 变量名 | 当前值 | 用途 |
|--------|--------|------|
| `--font-size-base` | `14px` | 基础字体（正文、按钮） |
| `--font-size-small` | `12px` | 小字体（辅助文字） |
| `--font-size-large` | `16px` | 大字体（标题） |
| `--font-size-xl` | `18px` | 超大字体（主标题） |

### 控件高度 (Control Height)

| 变量名 | 当前值 | 用途 |
|--------|--------|------|
| `--control-height` | `32px` | 标准高度（按钮、输入框） |
| `--control-height-small` | `28px` | 小高度 |
| `--control-height-large` | `36px` | 大高度 |

### 行高 (Line Height)

| 变量名 | 当前值 | 用途 |
|--------|--------|------|
| `--line-height-base` | `1.5` | 标准行高 |
| `--line-height-tight` | `1.4` | 紧凑行高 |
| `--line-height-loose` | `1.6` | 宽松行高 |

### 间距 (Spacing)

| 变量名 | 当前值 | 用途 |
|--------|--------|------|
| `--spacing-xs` | `4px` | 极小间距 |
| `--spacing-sm` | `8px` | 小间距 |
| `--spacing-md` | `16px` | 标准间距 |
| `--spacing-lg` | `24px` | 大间距 |
| `--spacing-xl` | `32px` | 超大间距 |
| `--spacing-xxl` | `48px` | 极大间距 |

---

## 🧩 组件样式

### 按钮 (Button)

- **主按钮**: 蓝色背景 (`--primary-color`)，白色文字
- **默认按钮**: 白色背景，蓝色边框
- **圆角**: `4px`
- **高度**: `32px`
- **无阴影**: `box-shadow: none`

### 输入框 (Input)

- **边框**: `1px solid var(--border-color)`
- **圆角**: `4px`
- **高度**: `32px`
- **聚焦**: 边框变蓝，淡蓝色阴影

### 表格 (Table)

- **表头背景**: `#F5F7FA`
- **表头文字**: `#303133`，加粗
- **边框**: `#DCDFE6`
- **行悬停**: 浅灰色背景

### 菜单 (Menu) 和侧边栏 (Sider)

#### 侧边栏容器
- **背景**: `#FFFFFF` (白色)
- **边框**: `1px solid #DCDFE6` (右侧边框)
- **宽度**: `256px` (展开状态)
- **宽度**: `80px` (折叠状态)

#### 菜单项
- **高度**: `40px`
- **内边距**: `8px 16px`
- **圆角**: `2px`
- **字体大小**: `14px`

#### 菜单状态
- **默认文字**: `#606266` (灰色)
- **悬停背景**: `#F5F7FA` (浅灰)
- **悬停文字**: `#3A71FF` (蓝色)
- **选中背景**: `#ECF5FF` (淡蓝色)
- **选中文字**: `#3A71FF` (蓝色)，加粗

#### 子菜单
- **缩进**: `32px` (左侧内边距)
- **父菜单标题**: 加粗，悬停时变蓝

### 标签页 (Tabs)

- **选中文字**: `#3A71FF` (蓝色)
- **下划线**: `#3A71FF` (蓝色)

---

## 🔧 如何修改样式

### 方法 1: 修改 CSS 变量

编辑 `src/styles/tokens.css` 文件，修改对应的 CSS 变量值：

```css
:root {
  --primary-color: #你的颜色值;
  --border-radius-base: 你的圆角值;
  /* ... 其他变量 */
}
```

### 方法 2: 修改 Ant Design 主题配置

编辑 `src/contexts/color-mode/index.tsx` 文件，在 `themeTokensBase` 和 `themeTokensLight` 中修改对应的值：

```typescript
const themeTokensBase = {
  colorPrimary: "#你的颜色值",
  borderRadius: 你的圆角值,
  // ... 其他配置
};
```

### 方法 3: 添加自定义样式

创建新的 CSS 文件（如 `src/styles/custom.css`），并在 `src/index.tsx` 中导入：

```typescript
import "./styles/custom.css";
```

---

## 📝 样式配置文件位置

- **CSS 变量定义**: `src/styles/tokens.css`
- **Ant Design 主题配置**: `src/contexts/color-mode/index.tsx`
- **样式配置参考**: `src/styles/theme-config.css` (此文件)

---

## 💡 设计建议

1. **保持一致性**: 修改颜色时，确保相关组件颜色也同步更新
2. **对比度**: 确保文字颜色与背景有足够的对比度（WCAG 标准）
3. **可访问性**: 考虑色盲用户，不要仅用颜色传达信息
4. **测试**: 修改后在不同设备和浏览器上测试

---

## 🎯 快速调整示例

### 更改主色调为绿色

```css
/* src/styles/tokens.css */
--primary-color: #52c41a;
--primary-hover: #73d13d;
--primary-bg-light: #f6ffed;
```

```typescript
// src/contexts/color-mode/index.tsx
colorPrimary: "#52c41a",
colorPrimaryHover: "#73d13d",
```

### 更改圆角为更圆润

```css
/* src/styles/tokens.css */
--border-radius-base: 8px;
```

```typescript
// src/contexts/color-mode/index.tsx
borderRadius: 8,
```

---

如有任何问题，请参考 Ant Design 官方文档：https://ant.design/docs/react/customize-theme-cn
